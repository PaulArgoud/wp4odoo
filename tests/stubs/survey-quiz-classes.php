<?php
/**
 * Survey & Quiz plugin stubs for PHPUnit tests.
 *
 * Provides constants for Quiz Maker (Ays) and QSM detection.
 *
 * @package WP4Odoo\Tests
 */

if ( ! defined( 'QUIZ_MAKER_VERSION' ) ) {
	define( 'QUIZ_MAKER_VERSION', '6.5.0' );
}

if ( ! defined( 'QSM_PLUGIN_INSTALLED' ) ) {
	define( 'QSM_PLUGIN_INSTALLED', true );
}
